<!-- It is crucial to read this before trying to run the program, because I have used a bit of a counter-intuitive structure when writing it -->

# Stress Testing for 3-D Gaussian Splatting Models

> **Note:** It is mandatory to go through these before trying to run main.py

1. First install all dependencies.
2. Navigate to the *StressTesting* directory
3. Run the command:

'''bash
pip install -e .